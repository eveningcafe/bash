---

- name: Check ha config for service
  shell: |
    cat {{ docker_shared_conf_dir }}/haproxy/haproxy/haproxy.cfg | grep cinder 
  register: service_cfg
  ignore_errors: yes
  
- include_role:
    name: haproxy
    tasks_from: update
  vars:
    services:
      - name: cinder_api
        port: 8776
        bind_ips:
        options:
          - "http-request del-header X-Forwarded-Proto"
  when: service_cfg.rc == 1
