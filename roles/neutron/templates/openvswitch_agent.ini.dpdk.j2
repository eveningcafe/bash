[DEFAULT]

[agent]
tunnel_types = vxlan
l2_population = True

[ovs]
bridge_mappings = {% for map in bridge_mappings %}{{ map.network }}:{{ map.bridge }}{% if not loop.last %},{% endif %}{% endfor %}

local_ip = {{ network_env.TUNNEL_IP }} 
vhostuser_socket_dir = /run/openvswitch/
datapath_type = netdev
tunnel_bridge = br-tun

[securitygroup]
firewall_driver = iptables_hybrid

[xenapi]
